<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbiter.oes.system.mapper.SysPermissionMapper">

    <resultMap id="BaseResultMap" type="com.rabbiter.oes.system.entity.SysPermission">
        <!--@Table sys_permission-->
        <result property="permissionId" column="permission_id" jdbcType="BIGINT"/>
        <result property="permissionName" column="permission_name" jdbcType="VARCHAR"/>
        <result property="resource" column="resource" jdbcType="VARCHAR"/>
        <result property="action" column="action" jdbcType="VARCHAR"/>
        <result property="hasDelete" column="is_delete" jdbcType="TINYINT" javaType="Boolean"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="DATE"/>
        <result property="modifiedBy" column="modified_by" jdbcType="VARCHAR"/>
        <result property="modifiedTime" column="modified_time" jdbcType="DATE"/>
    </resultMap>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select permission_id, permission_name, resource, action, is_delete, create_by, create_time, modified_by, modified_time
        from sys_permission
        <where>
            <if test="permissionId != null">
                and permission_id = #{permissionId}
            </if>
            <if test="permissionName != null and permissionName != ''">
                and permission_name = #{permissionName}
            </if>
            <if test="resource != null and resource != ''">
                and resource = #{resource}
            </if>
            <if test="action != null and action != ''">
                and action = #{action}
            </if>
            <if test="isDelete != null">
                and is_delete = #{isDelete}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="modifiedBy != null and modifiedBy != ''">
                and modified_by = #{modifiedBy}
            </if>
            <if test="modifiedTime != null">
                and modified_time = #{modifiedTime}
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(*)
        from sys_permission
        <where>
            <if test="permissionId != null">
                and permission_id = #{permissionId}
            </if>
            <if test="permissionName != null and permissionName != ''">
                and permission_name = #{permissionName}
            </if>
            <if test="resource != null and resource != ''">
                and resource = #{resource}
            </if>
            <if test="action != null and action != ''">
                and action = #{action}
            </if>
            <if test="isDelete != null">
                and is_delete = #{isDelete}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="modifiedBy != null and modifiedBy != ''">
                and modified_by = #{modifiedBy}
            </if>
            <if test="modifiedTime != null">
                and modified_time = #{modifiedTime}
            </if>
        </where>
    </select>

    <insert id="insertBatch" keyProperty="permissionId" useGeneratedKeys="true">
        insert into sys_permission(permission_name, resource, action, is_delete, create_by, create_time, modified_by, modified_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.permissionName}, #{entity.resource}, #{entity.action}, #{entity.isDelete}, #{entity.createBy}, #{entity.createTime}, #{entity.modifiedBy}, #{entity.modifiedTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="permissionId" useGeneratedKeys="true">
        insert into sys_permission(permission_name, resource, action, is_delete, create_by, create_time, modified_by, modified_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.permissionName}, #{entity.resource}, #{entity.action}, #{entity.isDelete}, #{entity.createBy}, #{entity.createTime}, #{entity.modifiedBy}, #{entity.modifiedTime})
        </foreach>
        on duplicate key update
        permission_name = values(permission_name),
        resource = values(resource),
        action = values(action),
        is_delete = values(is_delete),
        create_by = values(create_by),
        create_time = values(create_time),
        modified_by = values(modified_by),
        modified_time = values(modified_time)
    </insert>

</mapper>

